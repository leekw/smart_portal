Ext.define('Ext.dlog.controller.DLog', {
    extend: 'Ext.app.Controller',
    stores: ['DLogDetail'],
    models: ['DLogDetail'],

    views: [],

    refs: [
    ],
    
    init: function() {

        this.control({
        });
    }
});
